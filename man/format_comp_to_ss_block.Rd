% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_comp_to_ss_block.R
\name{format_comp_to_ss_block}
\alias{format_comp_to_ss_block}
\title{Format composition data to a Stock Synthesis (.ss) comp block}
\usage{
format_comp_to_ss_block(
  data,
  bins,
  bin_col = "AGE",
  comp_type = c("age", "len"),
  sexed = TRUE,
  ageerr = 1L,
  lbin_lo = NULL,
  lbin_hi = NULL,
  bin_fun = BIN_LEN_DATA,
  survey_label_fun = function(z) {
     paste0("FISH_", z$REGION_GRP, "_", z$SEASON)
 },
  fishery_label_fun = function(z) {
     paste0("FISH_", z$REGION_GRP, "_", z$GEAR, "_",
    z$SEASON)
 },
  file = NULL
)
}
\arguments{
\item{data}{Nested list containing survey and fishery composition data.}

\item{bins}{Numeric vector of bins to include in the output.}

\item{bin_col}{Character string naming the source column holding age or length.}

\item{comp_type}{Either "age" or "len".}

\item{sexed}{Logical; if TRUE output female and male compositions separately.}

\item{ageerr}{Integer age-error definition used for age comps only.}

\item{lbin_lo}{Optional low bin value written to SS block; defaults to min(bins).}

\item{lbin_hi}{Optional high bin value written to SS block; defaults to max(bins).}

\item{bin_fun}{Optional function used to bin raw length data before formatting.
It must take arguments \code{data} and \code{len_bins} and return a data.frame/data.table
containing a column named in \code{bin_col}.}

\item{survey_label_fun}{Function to create survey fleet labels.}

\item{fishery_label_fun}{Function to create fishery fleet labels.}

\item{file}{Optional output file path.}
}
\value{
A list with formatted tables, fleet map, and optionally written file.
}
\description{
Generalized formatter for age or length composition data for Stock Synthesis.
For age data, the source bin column is used directly. For length data, a
user-supplied binning function can be applied before aggregation.
}
