% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EWAA_utils.R
\name{make_wtatage_plots}
\alias{make_wtatage_plots}
\title{Write EWAA plots to disk}
\usage{
make_wtatage_plots(
  plots = 1:6,
  data,
  counts,
  lengths = NULL,
  dir = getwd(),
  year = format(Sys.Date(), "\%Y"),
  maxage = 15
)
}
\arguments{
\item{plots}{Integer vector specifying which plots to create (default 1:6).}

\item{data}{Wide EWAA data (weights) with first row as age-mean (see Details).}

\item{counts}{Wide EWAA counts aligned with \code{data}.}

\item{lengths}{Optional wide EWAA lengths aligned with \code{data}.}

\item{dir}{Output directory (default getwd()).}

\item{year}{Year label used in filenames (default current year).}

\item{maxage}{Plus-group age.}
}
\description{
Produces up to six plots:
\enumerate{
\item raw (no interpolation) heatmap + optional numbers plot,
\item interpolated heatmap,
\item interpolated + extrapolated heatmap,
\item same as (3) with shaded interpolations,
\item same as (3) with bold interpolations,
\item length-at-age heatmap (+ optional numbers) if \code{lengths} supplied.
}
}
\details{
This function expects \code{data} and \code{counts} to include an \strong{extra first row}
representing the mean across years (as used in some legacy EBS PCOD workflows).
If your \code{weight_at_age_wide()} output does not include this mean row, prepend it
before calling, or adapt the plotting call to pass \code{meanvec} explicitly.
}
