% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cond_length_age_corFISH.r
\name{cond_length_age_corFISH}
\alias{cond_length_age_corFISH}
\title{Conditional age-at-length compositions for fishery data (catch/age table)}
\usage{
cond_length_age_corFISH(
  con,
  species,
  area,
  max_age1,
  len_bins1,
  one_fleet = TRUE,
  wt = 1,
  seas = 1,
  ageerr = 1
)
}
\arguments{
\item{con}{DBI connection for AKFIN.}

\item{species}{Numeric species code(s) used in the SQL filter (e.g., fishery species code).}

\item{area}{Character region selector: one of \code{"AI"}, \code{"BS"}, \code{"GOA"}.}

\item{max_age1}{Maximum age (plus group).}

\item{len_bins1}{Numeric vector of length bin lower edges (SS bins). The final bin is treated as plus.}

\item{one_fleet}{Logical. If TRUE, all fishery data are assigned to fleet 1. If FALSE, HAL=2, POT=3, TRW/other=1.}

\item{wt}{Numeric multiplier applied to Nsamp (column 9).}

\item{seas}{Integer season for SS output (default 1 for fishery conditional age-at-length tables).}

\item{ageerr}{Integer age-error definition index for SS output (default 1).}
}
\value{
A list with element \code{$norm}, a data.frame in SS agecomp format with columns:
\code{Yr, Seas, Flt, Gender, Part, Ageerr, Lbin_lo, Lbin_hi, Nsamp, a0..aMax}.
}
\description{
Pulls domestic fishery age/length records via SQL, bins lengths, plus-groups ages,
and returns Stock Synthesis age-composition observations in conditional age-at-length form
(rows are Year x Length-bin; cells are age proportions). Sample sizes are weighted by \code{wt}.
}
